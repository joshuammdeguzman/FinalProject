<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="joshua.deguzman.com.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icslogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".studentForm" />
        <activity android:name=".guestForm" />
        <activity android:name=".shopHome" />
        <activity android:name=".shirtOrder" />
        <activity android:name=".jacketOrder" />
        <activity android:name=".lanyardOrder" />
        <activity android:name=".capOrder"></activity>
    </application>

</manifest>